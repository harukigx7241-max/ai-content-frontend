# ── コア ────────────────────────────────────────────────
fastapi>=0.135.0
uvicorn[standard]>=0.44.0
pydantic>=2.0.0

# ── テンプレート ─────────────────────────────────────────
jinja2>=3.1.0

# ── 認証・セキュリティ ─────────────────────────────────
bcrypt>=4.0.0
python-multipart>=0.0.9

# ── データベース ─────────────────────────────────────────
sqlalchemy>=2.0.0
# PostgreSQL を使う場合は以下を追加:
# psycopg2-binary>=2.9.0

# ── オプション: Stripe 課金 (ENABLE_BILLING=true の場合のみ必要) ──
# stripe>=7.0.0

# ── オプション: LLM API (ANTHROPIC_API_KEY / OPENAI_API_KEY を設定する場合のみ必要) ──
# いずれかコメント解除して pip install -r requirements.txt を再実行:
# anthropic>=0.25.0          # Anthropic Claude API (推奨)
# openai>=1.50.0             # OpenAI GPT API
# google-generativeai>=0.5.0 # Google Gemini API
